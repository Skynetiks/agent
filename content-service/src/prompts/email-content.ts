type GetEmailContentPromptProps = {
  senderCompanyInfo: string;
  prompt: string;
  receiverCompanyInfo: string;
};

export const getEmailContentPrompt = ({
  prompt,
  receiverCompanyInfo,
  senderCompanyInfo,
}: GetEmailContentPromptProps) => {
  return `

    `;
};
